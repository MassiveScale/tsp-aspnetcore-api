// <auto-generated/>
// This file was auto-generated by @massivescale/tsp-aspnetcore-api. Do not edit manually.

#pragma warning disable CS1591 // Missing XML comment for publicly visible type or member
#pragma warning disable CS0612 // Type or member is obsolete
#pragma warning disable CS0618 // Type or member is obsolete

using FluentValidation;
using Microsoft.AspNetCore.Http;
{{#if namespace}}

namespace {{{namespace}}};
{{/if}}

/// <summary>
/// Auto-generated version-aware PATCH validator for <see cref="{{{patchBodyTypeName}}}"/>.
/// Each rule fires only when the corresponding property is present in the patch and
/// is conditioned on the API version resolved from the inbound HTTP request
/// (URL segment, <c>api-version</c> header, or <c>api-version</c> query parameter).
/// Extend this partial class to add custom validation rules.
/// </summary>
public partial class {{{modelName}}}PatchValidator : AbstractValidator<{{{patchBodyTypeName}}}>
{
    private static readonly string[] _versions = new[] { {{#each allVersions}}"{{this}}"{{#unless @last}}, {{/unless}}{{/each}} };

    /// <summary>
    /// Initializes a new instance of <see cref="{{{modelName}}}PatchValidator"/> with version-aware auto-generated rules.
    /// </summary>
    public {{{modelName}}}PatchValidator(IHttpContextAccessor httpContextAccessor{{#each referencedValidators}}, AbstractValidator<{{{modelName}}}> {{{paramName}}}{{/each}})
    {
        var _apiVersion = ResolveApiVersion(httpContextAccessor.HttpContext, "{{{defaultVersion}}}");

{{#each baseProperties}}
{{#if notEmpty}}
        // {{{name}}} is required when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && (value?.ToString()?.Length ?? 0) > 0)
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if minLength}}
        // {{{name}}} must be at least {{minLength}} characters when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && (value?.ToString()?.Length ?? 0) >= {{minLength}})
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if maxLength}}
        // {{{name}}} cannot exceed a MaxLength of {{maxLength}} when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && ((value?.ToString()?.Length ?? 0) <= {{maxLength}}))
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if pattern}}
        // {{{name}}} must match the pattern '{{pattern}}' when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && System.Text.RegularExpressions.Regex.IsMatch(value?.ToString() ?? string.Empty, @"{{pattern}}"))
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if emailAddress}}
        // {{{name}}} must be a valid email address when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && System.Net.Mail.MailAddress.TryCreate(value?.ToString() ?? string.Empty, out _))
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if minValue}}
        // {{{name}}} must be greater than or equal to {{minValue.formatted}} when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && decimal.TryParse(value?.ToString(), out decimal n) && n >= {{minValue.formatted}}m)
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if maxValue}}
        // {{{name}}} cannot exceed a MaxValue of {{maxValue.formatted}} when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && decimal.TryParse(value?.ToString(), out decimal n) && n <= {{maxValue.formatted}}m)
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if isInEnum}}
        // {{{name}}} must be a valid enum value when provided
        this.RuleFor(x => x)
            .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && value != null && Enum.IsDefined(typeof({{{enumTypeName}}}), value))
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
            .WithName("{{{name}}}");

{{/if}}
{{#if referencedModelName}}
{{#if isCollectionReference}}
        // {{{name}}} items are validated recursively when provided
        RuleForEach(x => x.{{{name}}})
            .SetValidator({{{referencedParamName}}})
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)));

{{else}}
        // {{{name}}} is validated recursively when provided
        RuleFor(x => x.{{{name}}})
            .SetValidator({{{referencedParamName}}})
            .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)));

{{/if}}
{{/if}}
{{/each}}
{{#each versionGroups}}
        // Rules added in {{{sinceVersion}}}
        When(() => IsAtLeast("{{{sinceVersion}}}", _apiVersion, _versions), () =>
        {
{{#each properties}}
{{#if notEmpty}}
            // {{{name}}} is required when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && (value?.ToString()?.Length ?? 0) > 0)
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if minLength}}
            // {{{name}}} must be at least {{minLength}} characters when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && (value?.ToString()?.Length ?? 0) >= {{minLength}})
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if maxLength}}
            // {{{name}}} cannot exceed a MaxLength of {{maxLength}} when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && ((value?.ToString()?.Length ?? 0) <= {{maxLength}}))
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if pattern}}
            // {{{name}}} must match the pattern '{{pattern}}' when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && System.Text.RegularExpressions.Regex.IsMatch(value?.ToString() ?? string.Empty, @"{{pattern}}"))
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if emailAddress}}
            // {{{name}}} must be a valid email address when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && System.Net.Mail.MailAddress.TryCreate(value?.ToString() ?? string.Empty, out _))
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if minValue}}
            // {{{name}}} must be greater than or equal to {{minValue.formatted}} when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && decimal.TryParse(value?.ToString(), out decimal n) && n >= {{minValue.formatted}}m)
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if maxValue}}
            // {{{name}}} cannot exceed a MaxValue of {{maxValue.formatted}} when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && decimal.TryParse(value?.ToString(), out decimal n) && n <= {{maxValue.formatted}}m)
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if isInEnum}}
            // {{{name}}} must be a valid enum value when provided
            this.RuleFor(x => x)
                .Must(x => x.TryGetPropertyValue("{{{name}}}", out object value) && value != null && Enum.IsDefined(typeof({{{enumTypeName}}}), value))
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)))
                .WithName("{{{name}}}");

{{/if}}
{{#if referencedModelName}}
{{#if isCollectionReference}}
            // {{{name}}} items are validated recursively when provided
            RuleForEach(x => x.{{{name}}})
                .SetValidator({{{referencedParamName}}})
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)));

{{else}}
            // {{{name}}} is validated recursively when provided
            RuleFor(x => x.{{{name}}})
                .SetValidator({{{referencedParamName}}})
                .When(x => x.GetChangedPropertyNames().Any(p => p.Equals("{{{name}}}", StringComparison.OrdinalIgnoreCase)));

{{/if}}
{{/if}}
{{/each}}
        });

{{/each}}
        ExtendRules();
    }

    private static string ResolveApiVersion(HttpContext? context, string defaultVersion)
    {
        if (context is null) return defaultVersion;
        if (context.Request.RouteValues.TryGetValue("version", out var rv) && rv is string rs && rs.Length > 0) return rs;
        if (context.Request.Headers.TryGetValue("api-version", out var hv) && hv.Count > 0) return hv[0] ?? defaultVersion;
        if (context.Request.Query.TryGetValue("api-version", out var qv) && qv.Count > 0) return qv[0] ?? defaultVersion;
        return defaultVersion;
    }

    private static bool IsAtLeast(string target, string current, string[] orderedVersions)
    {
        var ti = System.Array.IndexOf(orderedVersions, target);
        var ci = System.Array.IndexOf(orderedVersions, current);
        return ci >= 0 && ti >= 0 && ci >= ti;
    }

    /// <summary>
    /// Override this method to add custom validation rules beyond the auto-generated ones.
    /// </summary>
    protected virtual void ExtendRules() { }
}

#pragma warning restore CS0612 // Type or member is obsolete
#pragma warning restore CS1591 // Missing XML comment for publicly visible type or member
#pragma warning restore CS0618 // Type or member is obsolete
